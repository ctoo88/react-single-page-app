@import './common';

.app {
  text-align: center;
}

.app-logo {
  height: 80px;
  background: url('./logo.svg') no-repeat 50%;
}

.app-header {
  background-color: #222;
  height: 150px;
  padding: 20px;
  color: white;
}

.app-title {
  font-size: 1.5em;
}

.app-intro {
  font-size: large;
}
